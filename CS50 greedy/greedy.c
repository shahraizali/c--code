#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int given;
    int original;
    int bakaya;
    int x=0;
    while(x=1){
            system("cls");
    printf("for how much have you done shopping? ");
    scanf("%d",&given);
    printf("what was the total price of products? ");
    scanf("%d",&original);
    printf("you bakaya: %d",given-bakaya);
system("pause");
}
}

